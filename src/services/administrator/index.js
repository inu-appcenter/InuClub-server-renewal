const AdminService = {
  login: async ({ adminId, password }) => {
    try {
      return 1;
    } catch (error) {
      throw new Error(error);
    }
  },
  signup: ({}) => {
    try {
    } catch (error) {
      throw new Error(error);
    }
  },
};

module.exports = AdminService;
